线程的三大特性   
    原子性: 即一个操作或者多个操作，要么全部执行并且执行的过程不会被任何原因打断，要么都不执行
    可见性:当多个线程访问一个变量时，一个线程修改了这个变量的值，其他线程能够立即得到修改后的值
    有序性:线程可以按照顺序去完成
Java内存模型:
  java内存模型简称jmm,定义了一个线程对另外一个线程可见，共享变量存放在主内存中，每个线程都有自己的本地内存
  当多个线程同时访问一个数据的时候，可能本地内存没有及时的刷新到主内存，所以就会发生线程不安全    
    